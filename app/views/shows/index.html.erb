<h1>All Shows</h1>

<% @shows.each do |show| %>
  <h3><%= link_to "#{show.name}", "/shows/#{show.id}" %></h3>
  <p>Daytime Show:<%= show.daytime_show %></p>
  <p>Number of Seasons:<%= show.num_of_seasons %></p>
<% end %>

